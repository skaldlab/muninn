version: 1
fail-on: critical

scanners:
  gitleaks:
    enabled: true
  zizmor:
    enabled: true
  actionlint:
    enabled: true
  poutine:
    enabled: true
  semgrep:
    enabled: true
    rulesets:
      - p/security-audit
      - p/secrets
  osv-scanner:
    enabled: true
  trivy:
    enabled: true
    severity: [CRITICAL, HIGH]
    ignore-unfixed: true
  checkov:
    enabled: true

suppressions:
  - id: testdata/fixture-repo
    reason: >
      Intentional vulnerable fixtures for integration testing.
      All secrets are fake, all vulnerabilities are deliberate.
    expires: ""
