# TEMP: poutine PR comment smoke test — remove before merge.
# Unpinned actions are flagged by zizmor, not poutine v1.1.6; injection is.
name: Poutine smoke test
on:
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          persist-credentials: false
      - name: Echo input
        run: echo ${{ github.event.pull_request.title }}
